# 28장 — 에이전트가 스스로 그래프를 넓히다

`5부 — 두 그래프가 만나는 곳` | [책 전체 목차](../../../README.md) | [출처 링크 모음](../../../SOURCES.md)

> 그래프를 열어 보니 「박민수」가 세 명이었습니다.

미리 말씀드리면, 이 장의 절반은 *하지 말아야 할 것*입니다. 자동 확장은 매력적인데 대가가 큽니다. 그 대가를 먼저 알고 시작하시는 게 낫습니다.

## 이 장의 절

| 절 | 제목 |
|---|---|
| 28.1 | 검증 없이 쓰면 10턴이면 망가진다 |
| 28.2 | 쓰기 앞에 관문 넷 |
| 28.3 | 어디서 온 사실인가 |
| 28.4 | 아무도 시키지 않은 방향으로 자란다 |
| 28.5 | 어디까지 넓힐 것인가 |

## 한 장 요약

- 에이전트가 그래프에 쓰기 시작하면 품질 관리가 시스템의 일부가 됩니다. 검증 없이 쓰면 10턴이면 망가져요.
- 맞는 사실만 넣어도 그래프가 틀릴 수 있습니다. 이름이 다르면 노드가 갈라지거든요. 추출 정확도를 올리는 것으로는 안 고쳐집니다.
- 관문은 넷입니다. 확신도, 정규화, 스키마, 단일 값 충돌. 각각 다른 것을 잡으니 하나만 두면 나머지가 샙니다. 그리고 정규화가 중복 검사보다 먼저 돌아야 해요.
- 단일 값 충돌을 자동으로 덮어쓰지 마세요. 오답이 정답을 지웁니다. 둘 다 살려 두고 사람에게 보내세요.
- 모든 엣지에 출처를 달고, 추론할 때 「무엇에 기댔는지」를 같이 적으세요. 나중에는 복원 못 합니다. 추론 결과의 신뢰도는 근거 중 가장 약한 것보다 강할 수 없습니다.
- 그래프는 아무도 시키지 않은 방향으로 자랍니다. 제약이 「지워져서」가 아니라 「묽어져서」 사라져요. 사람이 20%를 넣어도 방향은 안 바뀝니다. 개별 사실이 아니라 *비율*에 개입하세요.
- 넓히기는 깊을수록 수확이 줄고 오답이 늡니다. 평소에는 얕게, 가끔 깊게 가되 결과는 검토 대기로 넣으세요.
- 그리고 자동 확장의 진짜 위험은 「틀린 사실」이 아니라 「멈출 줄 모르는 것」입니다. 노드 증가율에 상한을 두세요.

## 키워드와 1차 출처

| 키워드 | 상태 | 출처 |
|---|---|---|
| 지식 그래프 완성 | [실험] | [knowledge graph completion](https://arxiv.org/abs/2002.00388) |
| 출처 기록 | [표준] | [provenance](https://www.w3.org/TR/prov-o/) |
| 철회 | [사실상 표준] | [retraction](https://www.w3.org/TR/prov-o/#Invalidation) |
| 진리 유지 시스템 | [실험] | [truth maintenance](https://dl.acm.org/doi/10.1145/321978.321979) |
| 스키마 드리프트 | [사실상 표준] | [schema drift](https://www.w3.org/TR/shacl/) |
| 자기 학습 편향 | [실험] | [self-training bias](https://arxiv.org/abs/2305.17493) |
| 제약 검증 | [표준] | [SHACL validation](https://www.w3.org/TR/shacl/) |

상태 표시는 **[표준]** 공식 명세, **[사실상 표준]** 명세는 없지만 업계가 널리 쓰는 것, **[실험]** 아직 자리를 잡는 중이라는 뜻입니다.

## 예제 실행

<!-- 실행 가이드 시작 — 사람이 쓴 부분. gen-docs.py 가 건드리지 않는다. -->

확인 시점 2026년 8월. Python 3.9 이상.

```bash
cd content/ch28/code
pip install kuzu

python3 ex1_write_loop.py         # 검증 없이 쓰면 10턴 만에 이렇게 된다
python3 ex2_write_gate.py         # 관문 넷이 각각 다른 것을 잡는다
python3 ex3_provenance.py         # 출처를 철회하면 무엇이 같이 무너지나
python3 ex4_drift.py              # 스스로 쓰는 루프의 분포 편향 (의존성 없음)
python3 ex5_expansion_budget.py   # 어디까지 넓힐 것인가 (의존성 없음)
```

`ex4` 는 시드를 3으로 고정했습니다. 시드를 바꾸면 숫자가 몇 %p 움직이는데
«제약이 줄고 사건이 는다»는 방향은 바뀌지 않습니다.

`ex5` 의 수확률과 정확도는 저희 도메인에서 잰 값을 단순화한 것입니다.
여러분 도메인에서는 홉별 정확도를 직접 재서 넣으세요.

<!-- 실행 가이드 끝 -->

---

**다음 장에서 합쳐지는 것:** 27장은 그래프를 읽었고 이 장은 썼습니다. 그런데 지금까지 둘을 «따로» 다뤘어요. 다음 장에서 하나의 백본으로 합칩니다. 그리고 합치고 나면 6부의 문제들이 전부 «같은 문제»였다는 게 드러납니다.

---

이전 [27장 에이전트에게 기억을 주는 가장 싼 방법](../../ch27/code/README.md) | [전체 목차](../../../README.md) | 다음 [29장 하나의 백본](../../ch29/code/README.md)

이 장에서 틀린 곳을 찾으셨다면 [사실 오류로](https://github.com/leaf-kit/book-graph-engineering/issues/new?template=01-fact-error.yml), 상태 라벨이 어긋났다면 [라벨 이의로](https://github.com/leaf-kit/book-graph-engineering/issues/new?template=03-status-label.yml) 적어 주세요.
