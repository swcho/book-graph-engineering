Create a clean, flat-design educational infographic explaining how transitive clustering (union-find) spreads a single bad merge across an entire cluster in entity resolution.

Title at the top center, large bold sans-serif: "Transitivity Is Not Free" with smaller Korean subtitle underneath: "이행성은 공짜가 아니다".

Layout: a horizontal 3-panel progression across the full width, left to right, with two large right-pointing arrows between the panels so the eye flows Panel 1 -> Panel 2 -> Panel 3. Each panel is a rounded white card with a thin border, a small numbered badge in its top-left corner (1, 2, 3), and a short bold caption bar at the bottom of the card. Below the three panels, a single full-width footer strip.

PANEL 1 — caption bar text: "Two correct clusters". Inside: two clearly separated node-edge graph groups. Left group: two blue circular nodes labeled "r06" and "r06b", joined by a thick solid green edge labeled "1.000". A small blue pill label above this group reads "Biz-no 222". Right group: two teal circular nodes labeled "r07" and "r07b", joined by a thick solid green edge labeled "0.873". A small teal pill label above reads "Biz-no 555". A vertical dashed grey divider line runs between the two groups with a tiny grey label "not similar / 0.42".

PANEL 2 — caption bar text: "One wrong edge". Same four nodes in the same positions, but now a single thick bright red edge connects "r06" to "r07" across the divider, labeled "0.550". A red warning triangle icon sits on this edge. A small red callout box points at it with the label "Address matched, biz-no differs". The divider dashed line is now broken.

PANEL 3 — caption bar text: "One polluted cluster". All four nodes now sit inside one large red-tinted rounded blob with a red dashed outline labeled "Merged cluster". All four nodes are recolored dull red. Three thin red DASHED edges are drawn between the previously unconnected pairs, each labeled "never verified". A bold red counter badge in the corner of this panel reads "4 false sameAs".

FOOTER STRIP — light neutral background, one short bold heading on the left: "Fixes / 완화 기법", and to its right four small icon+label chips laid out in a row: a ruler icon with "Cap cluster size", a grid icon with "Check inner density", a person icon with "Send to human review", and an undo-arrow icon with "Keep merges reversible".

Style: clean flat vector design, educational infographic, generous white space, light neutral background (#F7F8FA), restrained palette of blue, teal, green for correct states and a single strong red for the error state, grey for neutral lines, crisp thin strokes, simple geometric icons, no gradients, no 3D, no photographic textures, highly legible sans-serif typography, all labels short.

Aspect ratio: 16:9 landscape.
