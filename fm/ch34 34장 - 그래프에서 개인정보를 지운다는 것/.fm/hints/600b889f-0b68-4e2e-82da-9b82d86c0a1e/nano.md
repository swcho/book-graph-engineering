Create a clean flat-design educational infographic, 16:9 landscape, titled "DELETE node — 5 places data survives" with a smaller Korean subtitle "노드를 지워도 남는 다섯 곳".

Layout: one horizontal band across the top, then a wide main area split into two zones side by side, then a thin summary strip at the bottom. Reading flow goes left to right.

Top band: a small graph node drawn as a circle labeled "Person node", with a red X struck through it, and the caption "DELETE n — 1 row gone". A thick gray arrow points right into the main area with the label "but 5 traces remain".

Main area, left zone (light blue panel, header "INSIDE the database / 같은 DB 안"): two numbered cards.
Card 1, label "1. Incoming edges", icon: three small circles with arrows all pointing INTO a dashed empty circle, sublabel "someone else's history".
Card 2, label "2. Names in free text", icon: a document page with a highlighted line of text and a magnifier over it, sublabel "not a normalized field".

Main area, right zone (light amber panel, header "BEYOND the DELETE / DELETE가 닿지 않는 곳"): three numbered cards in a row.
Card 3, label "3. Event log", icon: a vertical stack of chevron blocks with a padlock, sublabel "append-only".
Card 4, label "4. Backups", icon: three stacked disk cylinders with a clock, sublabel "yesterday's snapshot".
Card 5, label "5. Search index", icon: a magnifying glass over a small grid outside a database cylinder, sublabel "copy outside the DB".

Bottom strip, two halves matching the zones above: left half reads "Fix with better queries", right half reads "Fix with design + process".

Style: clean flat vector infographic, educational poster, muted blue / amber / gray palette with one red accent for the deletion X, generous white space, thin rounded card outlines, soft drop shadows, crisp sans-serif typography, no photographic texture, no clutter. All labels short and exactly as specified.
