Educational infographic, clean flat design, 16:9 landscape aspect ratio, soft pastel background, crisp sans-serif typography.

Title at top center: "One Document Changes — What Must Be Redone?" with small Korean subtitle "문서 하나가 바뀌면?".

Layout: a single edited document icon (a page with a pencil and a small orange "changed!" badge) sits at the top center. Four arrows fan out downward from it to four equal side-by-side panels, guiding the eye left to right from most-local fix to zero work.

Panel 1 — light blue. Header: "Vector Only (벡터만)". Visual: a row of small text-chunk squares, one square highlighted and being re-encoded into a small number array, with a circular refresh arrow on just that one square; other squares stay gray and untouched. Label: "Re-embed that chunk". Small caption: "cheap, local fix".

Panel 2 — light green. Header: "Entity Graph (엔티티 그래프)". Visual: a small node-edge graph; a few edges from the changed document are shown dashed and crossed out, replaced by fresh solid edges snapping in. Label: "Swap that doc's triples". Small caption: "only its edges change".

Panel 3 — light orange. Header: "Community Summary (커뮤니티 요약)". Visual: clusters of nodes enclosed by dotted boundary bubbles; one boundary is wobbling and redrawn, and a summary document icon above it shows a refresh arrow. Label: "Boundary shifts, rewrite summary". Small caption: "expensive ripple effect".

Panel 4 — light purple. Header: "Query-Time Expansion (질의 시점 확장)". Visual: a calm empty desk with a checkmark and a "zzz" sleeping icon; a small magnifying glass labeled "work happens at query". Label: "Do nothing". Small caption: "no index to maintain".

Bottom strip: a thin horizontal gradient bar from purple (left end labeled "zero upkeep") to orange (right end labeled "heavy upkeep"), with the four approach names placed along it: Query-Time, Vector, Entity Graph, Community Summary.

Style: consistent icon set, generous white space, rounded panel corners, subtle drop shadows, no photorealism, all labels short and legible.
