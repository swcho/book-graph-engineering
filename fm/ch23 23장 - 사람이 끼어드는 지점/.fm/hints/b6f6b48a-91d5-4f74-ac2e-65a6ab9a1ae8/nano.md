Create a clean, modern educational infographic in landscape 16:9 format.

TITLE (top center, large bold sans-serif):
"Waiting for a Human: Loop vs Interrupt"
Subtitle in smaller grey text underneath: "while loop 대기 vs interrupt + checkpointer"

OVERALL LAYOUT: two vertical panels of equal width, side by side, separated by a thin vertical divider line with a small "VS" badge in the middle. A single full-width summary bar runs across the bottom. Eye flow goes left panel top-to-bottom, then right panel top-to-bottom, then down to the summary bar.

LEFT PANEL — warm red/orange accent color, header label "WHILE LOOP" with a small subtitle "프로세스 점유".
Show a vertical 3-step flow with downward arrows:
1. A server box icon labeled "Process holds thread", with a spinning circular-arrow loop icon wrapped around it and a small tag "sleep(5) forever".
2. A memory chip icon holding a small state card, labeled "State lives in RAM".
3. A server box with a lightning/crash symbol and a red X, labeled "Restart = state lost".
Bottom of this panel: a red-tinted callout strip reading "Waiting = running code".

RIGHT PANEL — cool teal/blue accent color, header label "INTERRUPT" with a small subtitle "프로세스 해제".
Show a vertical 3-step flow with downward arrows:
1. A node box with a pause symbol labeled "Node pauses, call returns", and a small outgoing dashed arrow to a freed server box marked with a green check and the tag "Process released".
2. A cylinder database icon labeled "Checkpointer" with a saved state card inside it and a key tag "thread_id".
3. Two different server boxes (drawn as new machines) with a curved arrow coming up from the database into them, labeled "Any process resumes" and a small tag "Command(resume)".
Bottom of this panel: a teal-tinted callout strip reading "Waiting = stored data".

BOTTOM SUMMARY BAR (full width, dark background, white text, centered):
"Loop keeps the process. Interrupt keeps the state."
On the right end of this bar, a small amber warning chip with a caution triangle icon reading "Side effects go AFTER interrupt".

STYLE: clean flat design, educational infographic, minimal line icons with 2px strokes, generous white space, light neutral background, rounded rectangles, muted professional palette (red-orange vs teal-blue as the only saturated colors), crisp sans-serif typography, high contrast, no photorealism, no clutter, no gradients beyond subtle tints. All labels must be short and legible. Aspect ratio 16:9.
