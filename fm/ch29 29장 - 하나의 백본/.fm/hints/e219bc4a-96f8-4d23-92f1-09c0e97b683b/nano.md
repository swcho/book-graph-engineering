Educational infographic, clean flat design, 16:9 landscape, soft pastel palette with clear outlines, generous whitespace.

Title at top center, bold: "The Compromise Setup (절충 구성)" with small subtitle "What goes where?"

Layout: two large side-by-side panels connected by a thin dotted link line, plus a small comparison strip at the bottom.

LEFT PANEL — a rounded rectangle labeled "Graph DB (그래프)", drawn as a small network diagram:
- One highlighted circular node labeled "Run node" (한국어 병기: "Run 노드")
- Two or three thin arrows from the Run node pointing to small gray entity circles, arrows labeled "Reads edge" and "Writes edge"
- Caption under panel: "Links only (링크만)"
- Visual emphasis: this panel looks light and sparse — only a few elements.

RIGHT PANEL — a rounded rectangle labeled "Fast Store (빠른 저장소)", drawn as a database cylinder icon with logos-style text "SQLite / Postgres":
- Inside the cylinder, a thick stacked document icon labeled "State body" (한국어 병기: "상태 본문")
- Small tag on the document: "Checkpoint (체크포인트)"
- Visual emphasis: this panel looks heavy and dense — big bulky payload.

CENTER LINK — a dotted arrow between the two panels labeled "pointer, not payload".

BOTTOM STRIP — three small icons in a row showing the payoff:
1. Check mark icon, label "Cross-query OK"
2. Gauge icon at low level, label "Low write load"
3. Crossed-out icon, label "No single transaction"

Eye flow: title, then left graph panel, dotted link, right store panel, then bottom payoff strip. All labels 3-5 words max, crisp sans-serif typography, no long sentences.
