A clean flat-design educational infographic, 16:9 landscape, titled "AGENT TUNING ORDER".

Layout: a left-to-right ascending three-step staircase occupies the center, with a thin vertical bar chart docked on the far left and a single caption strip along the bottom. The eye moves left to right, from the tallest cost bar into STEP 1, then up the stairs.

FAR LEFT PANEL, header "WHERE TIME GOES": a slim stacked vertical bar, total labeled "1,919 ms". One huge dark segment fills 95% of the bar, labeled "MODEL CALL 94.8%". A tiny sliver at the top is labeled "EVERYTHING ELSE 5%". A dashed arrow leaves the huge segment and points at the STEP 1 stair.

CENTER STAIRCASE, three blocks rising left to right, each a rounded rectangle with a number badge:

Lowest-but-widest block, badge "1", deep teal, header "FEWER MODEL CALLS". Three small icons in a row with labels underneath: a database-disc icon "Cache", a branching-fork icon "Routing", a stop-sign icon "Loop exit". Below the icons a thick horizontal savings bar, nearly full width, labeled "up to 50% saved".

Middle block, badge "2", medium blue, header "FASTER MODEL CALLS". Three small icons: a compressed-document icon "Short prompt", a small-brain icon "Small model", a flowing-wave icon "Streaming". Savings bar about one third width, labeled "up to 28% saved".

Top block, badge "3", pale gray, header "EVERYTHING ELSE". Three small icons: a magnifier-over-tree icon "Query plan", a key icon "Index", a disk icon "Checkpoint". Savings bar a tiny stub, labeled "only 3% saved".

A bold curved arrow sweeps up along the staircase from block 1 to block 3, annotated "DO IN THIS ORDER".

TOP RIGHT CALLOUT BOX, muted amber, header "AMDAHL'S LAW". One formula line in mono type: "speedup = 1 / ((1-P) + P/S)". Under it two short lines: "P = 5% then max 1.05x" and "Fix the big slice first".

SMALL SIDE NOTE under the callout, header "STREAMING CAVEAT": two tiny bars, one labeled "TTFT: much lower", one labeled "Total time: same".

BOTTOM STRIP, dark band, single line of white text: "Optimizing the other 95% of code changes 3% of the time."

Style: clean flat design, educational infographic, generous white space, off-white background, restrained palette of deep teal, medium blue, pale gray, one amber accent, dark charcoal text. Simple geometric line icons, no gradients, no photographic elements, no 3D. Crisp sans-serif labels, all labels short. Aspect ratio 16:9.
