Create a clean educational infographic, 16:9 landscape aspect ratio, flat modern vector style, soft off-white background, generous whitespace, crisp sans-serif typography, color palette of deep navy, teal, amber, and coral red.

Title at top center, large bold text: "Shrinking the Replay Window" with smaller Korean subtitle "재실행 창 좁히기".

Below the title, a small intro strip: a horizontal timeline with two icons — a database cylinder labeled "our log (기록)" and a cloud API icon labeled "external call (호출)" — connected by a dashed gap highlighted in coral red labeled "unknown window (모름)". Caption in small text: "call and log are never one transaction".

Main body: three horizontal panels stacked vertically, each one a timeline lane with a left-side number badge and method name. Each lane shows event markers on a timeline arrow and a shaded coral-red band whose WIDTH represents the size of the unknown window — the bands must visibly shrink from panel 1 to panel 3.

Panel 1, badge "1", title "Record first, call later" (Korean: 기록 먼저): timeline markers "write STARTED" (navy dot), then "API call" (teal cloud dot), then "write DONE" (navy dot). Wide coral band spanning from STARTED to DONE, labeled "wide but detectable". Small human icon at right labeled "human closes it".

Panel 2, badge "2", title "Record right after call" (Korean: 호출 직후 기록): timeline markers "API call" (teal cloud dot) immediately followed by "write DONE" (navy dot), placed very close together. Narrow coral band between them labeled "narrow, not detectable". Small human icon at right labeled "human closes it".

Panel 3, badge "3", title "Query API on restart" (Korean: 재시작 때 조회): timeline shows a restart arrow icon, then a question bubble to a cloud API labeled "was it charged?", returning a green checkmark labeled "fact, not guess". Coral band nearly zero width, replaced by a green sliver labeled "window closed". Robot/gear icon at right labeled "auto, no human" with a small green check.

Bottom footer bar: three small comparison chips showing band widths side by side (wide red, thin red, green check) with caption "You cannot erase the window — only narrow it, and only a query API closes it automatically."

Keep all labels short (3-5 words), high contrast, textbook-diagram quality, no photorealism.
