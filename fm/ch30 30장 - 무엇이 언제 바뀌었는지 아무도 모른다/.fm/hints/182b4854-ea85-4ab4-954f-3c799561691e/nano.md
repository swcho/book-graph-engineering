Create a clean flat-design educational infographic, 16:9 landscape aspect ratio, titled "Interval Query vs Event Replay" with a smaller subtitle "State vs Change".

Overall layout: a large two-panel side-by-side comparison filling the middle of the canvas, with a thin divider line down the center labeled "same answer, different strengths". A single full-width band runs across the bottom. Reading flow: title at top, then eyes sweep left panel to right panel, then drop to the bottom band.

LEFT PANEL — cool blue palette. Panel header: "Interval Query". Subhead: "STATE". Visual element: a horizontal timeline with three stacked colored bars, each bar spanning a date range with small start and end markers, and a bold vertical dashed line cutting through all bars labeled "query at time T". Only the bars crossed by the dashed line are filled solid; the others are faded. Small tag on an edge: "vfrom / vto".
Below the visual, a short list under the heading "Answers well" with a check icon per line:
- "Who led on May 1?"
- "What was true then?"
- "Overlapping periods"
Then a heading "Cannot answer" with an X icon per line:
- "Who changed it?"
- "Why did it change?"
- "Was it reverted?"

RIGHT PANEL — warm orange palette. Panel header: "Event Replay". Subhead: "CHANGE". Visual element: a horizontal chain of five small event cards flowing left to right with arrows between them, each card showing a tiny icon plus short text like "add", "delete", "add", with small actor badges reading "human", "agent", "hr-sync". A camera-shutter style snapshot icon sits under the third card labeled "snapshot", and a bracket spans only the cards after it labeled "replay only the tail".
Below the visual, a heading "Answers well" with check icons:
- "Who made this change?"
- "When and why?"
- "Agent edits reverted"
Then a heading "Cannot answer cheaply" with a warning icon:
- "Current state, every time"

BOTTOM BAND — neutral gray-green. A left-to-right arrow diagram: a box labeled "Event Log" with the caption "source of truth", a thick arrow labeled "fold / reduce", and a box labeled "Interval Edges" with the caption "derived view". To the right of the arrow, a small crossed-out reverse arrow with the label "never the other way". Band caption at far right: "Keep both".

Style: clean flat design, educational infographic, generous white space, soft rounded rectangles, thin consistent line weights, muted blue and orange accent colors on an off-white background, simple geometric icons, modern sans-serif typography, no photorealism, no gradients, no drop shadows. All labels must be short — 3 to 5 words maximum. 16:9 aspect ratio.
