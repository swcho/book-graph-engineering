Create a clean flat-design educational infographic, 16:9 landscape, titled "WHEN Do You Count? (언제 세느냐)". Subtitle under the title: "Chunk Retrieval vs GraphRAG".

Layout: a vertical center divider splits the canvas into two mirrored columns. Left column header "CHUNK RETRIEVAL / 조각 검색" in cool blue. Right column header "GraphRAG / 그래프 RAG" in warm orange. Each column contains a horizontal left-to-right timeline arrow with two labeled stages: "INDEX TIME / 색인 시점" then "QUERY TIME / 질의 시점". A small clock icon marks the stage where the heavy counting happens — place the clock on QUERY TIME in the left column and on INDEX TIME in the right column, and glow that stage box.

Left column detail. Index-time box: 12 small grey document icons arranged in a grid, each with a tiny arrow to a dot in a scatter cloud; label "Embed each doc alone" and small tag "O(1) LLM calls". Query-time box: a magnifying glass pulling only 4 highlighted documents out of the 12, the other 8 shown faded with a dashed X over them; label "Read only top-k" and tag "O(k) read". Bottom result banner in muted red with an X mark: "Cannot count the whole".

Right column detail. Index-time box: the same 12 documents feeding into a small node-and-edge knowledge graph, the graph then enclosed by 3 colored community bubbles, each bubble folding into a small folded-paper summary card; label "Build graph, fold summaries" and tag "O(N) LLM calls". Query-time box: the 3 summary cards unfolding and merging into one ranked bar list; label "Unfold the summaries" and tag "O(C) read". Bottom result banner in green with a check mark: "Counted before the query".

Bottom full-width strip spanning both columns, in soft neutral grey, titled "Same idea as a database". Three side-by-side mini panels connected by equals signs: panel 1 shows a B-tree icon labeled "Index = top-k lookup"; panel 2 shows a table with a GROUP BY aggregate icon labeled "Full scan = global question"; panel 3 shows a stack of pre-computed rows labeled "Materialized view = folded summary". A small footnote line under the strip: "Cost moves, it does not vanish".

Visual flow: eyes travel left column top-to-bottom, then right column top-to-bottom, then across the bottom strip. Style: clean flat vector infographic, generous white space, thin 2px outlines, rounded corners, limited palette of blue, orange, green, red accent and neutral grey on off-white background, crisp sans-serif labels, no photorealism, no gradients, no clutter. All labels short and legible.
