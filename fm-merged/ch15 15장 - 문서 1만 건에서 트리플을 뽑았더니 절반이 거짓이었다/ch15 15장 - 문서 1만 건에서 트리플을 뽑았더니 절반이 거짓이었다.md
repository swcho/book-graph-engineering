# 15장 - 문서 1만 건에서 트리플을 뽑았더니 절반이 거짓이었다

`3부 - 지식 그래프 엔지니어링 (트랙 1)` | **한국어** | [English](../../../content_en/ch15/code/README.md) | [책 전체 목차](../../../README.md) | [출처 링크 모음](../../../SOURCES.md)

> 사내 위키 1만 2천 건에서 트리플을 뽑았습니다. 나흘 걸렸고 47만 개가 나왔어요.

더 아팠던 건 다음이었습니다. 무엇이 거짓인지 *알 방법이 없었어요*. 트리플에 근거가 안 붙어 있었거든요. 47만 개를 다 검수할 수도 없고, 그렇다고 반쯤 거짓인 걸 쓸 수도 없었습니다. 결국 전부 버리고 다시 만들었습니다. 이 장은 그 두 번째 시도에서 배운 것들입니다.

## 이 장의 절

| 절 | 제목 |
|---|---|
| 15.1 | 재지 않으면 개선했는지도 모른다 |
| 15.2 | 근거를 같이 내놓게 하라 |
| 15.3 | 보류함이 파이프라인의 심장이다 |
| 15.4 | 모델이 말하는 확신은 믿을 게 못 된다 |
| 15.5 | 문서가 바뀌면 무엇을 지울까 |

## 한 장 요약

- 먼저 재세요. 정답 40건을 손으로 만드는 이틀이 나머지 전부를 구합니다. 그리고 오류를 종류별로 나누세요. 「정밀도 0.64」만으로는 어디를 고칠지 모릅니다.
- 근거 문장을 같이 내놓게 하고 원문과 대조하세요. 문자열 비교라 싸고 결과가 매번 같습니다. 검사할 때는 주어가 아니라 *목적어*가 근거 문장에 있는지 보세요.
- 어휘 밖 술어는 버리지 말고 보류함에 넣으세요. 빈도 상위 10개만 주에 한 번 보면 됩니다. 그리고 보류율이 곧 「우리 어휘가 현실과 어긋나는 정도」의 지표입니다.
- 모델의 자기 보고 확신도는 거짓에도 높은 값을 줍니다. 여러 번 돌려 표를 세는 쪽이 낫고, 비싸니까 애매한 것에만 쓰세요.
- 재추출할 때 문서 단위로 통째 교체하면 사람이 정정한 값이 날아갑니다. 트리플마다 출처를 저장하세요. 문서, 위치, 누가, 언제. 넷이면 됩니다.
- 그리고 트리플 개수를 KPI로 두지 마세요. 제 2차 결과는 1차보다 33% 적었는데, 쓸 수 있는 개수로는 더 많았습니다.

## 키워드와 1차 출처

| 키워드 | 상태 | 출처 |
|---|---|---|
| 정보 추출 | [사실상 표준] | [information extraction](https://aclanthology.org/D19-1522/) |
| 근거 기반 생성 | [사실상 표준] | [grounded generation](https://arxiv.org/abs/2005.11401) |
| 자기 일관성 | [사실상 표준] | [self-consistency](https://arxiv.org/abs/2203.11171) |
| 출처 표현 | [표준] | [PROV-O](https://www.w3.org/TR/prov-o/) |
| 정밀도와 재현율 | [표준] | [precision, recall](https://www.iso.org/standard/35736.html) |
| 구조화 출력 | [사실상 표준] | [structured output](https://docs.claude.com/en/docs/agents-and-tools/tool-use/overview) |
| 그래프 추출 파이프라인 | [사실상 표준] | [Microsoft GraphRAG indexing](https://github.com/microsoft/graphrag) |

상태 표시는 **[표준]** 공식 명세, **[사실상 표준]** 명세는 없지만 업계가 널리 쓰는 것, **[실험]** 아직 자리를 잡는 중이라는 뜻입니다.

## 예제 실행

<!-- 실행 가이드 시작 - 사람이 쓴 부분. gen-docs.py 가 건드리지 않는다. -->

확인 시점 2026년 8월. Python 3.9 이상. **의존성 없음.**

```bash
cd content/ch15/code
python3 ex1_measure.py       # 정밀도와 재현율을 먼저 잰다
python3 ex2_grounding.py     # 근거 검사로 지어내기 거르기
python3 ex3_normalize.py     # 어휘 정규화와 보류함
python3 ex4_confidence.py    # 자기 보고 vs 여러 번 돌려 표 세기
python3 ex5_incremental.py   # 문서가 바뀌면 무엇을 지울까
```

`extractor.py` 는 API 키 없이 돌리려고 만든 **가짜 추출기**입니다.
진짜 모델이 흔히 보이는 실패 네 가지(지어내기, 어휘 벗어나기, 추측을 사실로, 누락)를
그대로 흉내 냅니다.

<!-- 실행 가이드 끝 -->

---

**다음 장에서 뒤집히는 것:** 이 장에서 「비활성 표시」와 「valid_to」를 여러 번 언급했습니다. 다음 장이 그 본편이에요. 어제는 맞았고 오늘은 틀린 사실을 그래프에 어떻게 담을지 봅니다.

---

이전 [14장 같은 사람이 노드 네 개로 앉아 있다](../../ch14/code/README.md) | [전체 목차](../../../README.md) | 다음 [16장 어제는 맞았고 오늘은 틀리다](../../ch16/code/README.md)

이 장에서 틀린 곳을 찾으셨다면 [사실 오류로](https://github.com/leaf-kit/book-graph-engineering/issues/new?template=01-fact-error.yml), 상태 라벨이 어긋났다면 [라벨 이의로](https://github.com/leaf-kit/book-graph-engineering/issues/new?template=03-status-label.yml) 적어 주세요.

---

## 예제 코드

### `code/docs.py`

```python
"""
15장 공통 — 문서 조각과 «사람이 확인한 정답 트리플». 의존성 없음.

정답을 손으로 적어 둔 게 이 장의 핵심이다.
정답 없이는 정밀도도 재현율도 못 잰다. 그리고 못 재면 개선했는지도 모른다.
"""

DOCS = [
    ("d01", "가온테크는 2025년 6월 2일 나루소프트와 유지보수 계약을 체결했다. "
            "계약번호는 C-2025-118이며, 담당자는 김하늘 부장이다."),
    ("d02", "라온에너지는 예산 삭감을 이유로 2024년 8월 계약을 해지했다."),
    ("d03", "다올물산의 대표는 최민준이다. 본사는 인천 연수구에 있다."),
    ("d04", "가온테크 김하늘 부장이 이번 분기 실적을 발표했다. "
            "나루소프트와의 협업이 언급되었다."),
    ("d05", "마루상사는 다올물산의 자회사로 알려져 있으나, 공시된 바는 없다."),
]

# 사람이 문서를 읽고 확정한 트리플. (문서, 주어, 술어, 목적어)
GOLD = {
    ("d01", "가온테크", "체결", "C-2025-118"),
    ("d01", "C-2025-118", "상대", "나루소프트"),
    ("d01", "C-2025-118", "체결일", "2025-06-02"),
    ("d01", "C-2025-118", "담당", "김하늘"),
    ("d02", "라온에너지", "해지", "2024-08"),
    ("d02", "라온에너지", "해지사유", "예산 삭감"),
    ("d03", "다올물산", "대표", "최민준"),
    ("d03", "다올물산", "소재지", "인천 연수구"),
    ("d04", "김하늘", "소속", "가온테크"),
    # d05 는 «알려져 있으나 공시된 바는 없다» — 사실로 확정할 수 없다. 정답에서 뺀다.
}

# 어휘 — 12장에서 정한 그것. 여기 없는 술어는 보류 대상이다.
VOCAB_PREDICATES = {"체결", "해지", "해지사유", "상대", "체결일", "담당",
                    "대표", "소재지", "소속"}
```

### `code/ex1_measure.py`

```python
"""
예제 1 — 정밀도와 재현율을 먼저 잰다. 재지 않으면 개선했는지 모른다.

    python3 ex1_measure.py

의존성 없음.
"""

from docs import DOCS, GOLD
from extractor import extract


def main():
    got = set()
    for did, _ in DOCS:
        for s, p, o, _ in extract(did):
            got.add((did, s, p, o))

    tp = got & GOLD
    fp = got - GOLD
    fn = GOLD - got
    prec = len(tp) / len(got) if got else 0
    rec = len(tp) / len(GOLD) if GOLD else 0
    f1 = 2 * prec * rec / (prec + rec) if prec + rec else 0

    print(f"뽑은 트리플 {len(got)}개, 정답 {len(GOLD)}개\n")
    print(f"맞음  {len(tp):>2}개")
    print(f"틀림  {len(fp):>2}개")
    print(f"놓침  {len(fn):>2}개")
    print(f"\n정밀도 {prec:.3f}  재현율 {rec:.3f}  F1 {f1:.3f}\n")

    print("틀린 것 — 왜 틀렸는지 종류를 나눠 보면")
    reasons = {
        ("d01", "C-2025-118", "계약체결일자", "2025-06-02"): "어휘 밖 술어 (뜻은 맞다)",
        ("d01", "가온테크", "업종", "IT 서비스"): "지어냄 (원문에 없다)",
        ("d04", "가온테크", "협업", "나루소프트"): "어휘 밖 + 근거가 약하다",
        ("d05", "마루상사", "모회사", "다올물산"): "추측을 사실로 (공시된 바 없다)",
    }
    for t in sorted(fp):
        print(f"    {t[1]:<12} {t[2]:<8} {t[3]:<14} — {reasons.get(t, '?')}")

    print("\n놓친 것")
    for t in sorted(fn):
        print(f"    {t[1]:<12} {t[2]:<8} {t[3]}")

    print(
        "\n정밀도 0.64는 «열에 서넛이 거짓»이라는 뜻이다.\n"
        "이 상태로 적재하면 질의가 거짓을 답한다. 그리고 사용자는 한 번 겪으면 안 쓴다.\n"
        "\n그런데 틀린 넷이 다 같은 종류가 아니다.\n"
        "  «계약체결일자»는 뜻이 맞다. 어휘에 매핑하면 정답이 된다.\n"
        "  «업종»과 «모회사»는 진짜 거짓이다. 버려야 한다.\n"
        "\n종류를 안 나누고 «정밀도 0.6»만 보면 무엇을 고칠지 모른다.\n"
        "오류를 분류하는 게 개선의 첫 단계다."
    )


if __name__ == "__main__":
    main()
```

### `code/ex2_grounding.py`

```python
"""
예제 2 — 근거 검사. 지어낸 것을 걸러 내는 제일 싼 방법.

    python3 ex2_grounding.py

의존성 없음.
모델에게 «근거 문장»을 같이 내놓으라고 시키고, 그 문장이 원문에 정말 있는지 대조한다.
"""

import re

from docs import DOCS, GOLD
from extractor import extract

TEXT = dict(DOCS)


def normalize(s):
    return re.sub(r"\s+", "", s or "")


def has_evidence(doc_id, quote):
    """근거 문장이 원문에 실제로 있는가."""
    return normalize(quote) in normalize(TEXT.get(doc_id, ""))


def supports(quote, subj, obj):
    """근거 문장이 그 트리플을 실제로 뒷받침하는가.

    핵심은 «목적어»다. 주어는 문서 전체에 흔히 나오지만,
    목적어가 근거 문장에 없으면 그 값은 문장에서 나온 게 아니다.
    이 규칙 하나가 지어내기를 제일 많이 잡는다."""
    q = normalize(quote)
    def core(x):
        x = normalize(x)
        return re.sub(r"[0-9\-]", "", x)[:2] or x[:2]
    return core(obj) in q


HEDGE = ["알려져", "추정", "듯", "로 보인다", "가능성", "일 수 있"]


def is_hedged(doc_id, quote):
    """원문에서 그 문장 주변에 «단정하지 않는» 표현이 있는가."""
    text = TEXT.get(doc_id, "")
    i = text.find(quote[:8])
    window = text[max(0, i - 20): i + len(quote) + 40] if i >= 0 else text
    return any(h in window for h in HEDGE)


def main():
    kept, dropped = [], []
    for did, _ in DOCS:
        for s, p, o, quote in extract(did):
            t = (did, s, p, o)
            if not has_evidence(did, quote):
                dropped.append((t, "근거 문장이 원문에 없다"))
            elif not supports(quote, s, o):
                dropped.append((t, "문장이 트리플을 뒷받침하지 않는다"))
            elif is_hedged(did, quote):
                dropped.append((t, "원문이 단정하지 않는다"))
            else:
                kept.append(t)

    print(f"{'판정':<6} {'트리플'}")
    print("-" * 62)
    for t in kept:
        print(f"{'통과':<6} {t[1]:<12} {t[2]:<10} {t[3]}")
    for t, why in dropped:
        print(f"{'버림':<6} {t[1]:<12} {t[2]:<10} {t[3]:<14} ← {why}")

    got = set(kept)
    tp = got & GOLD
    prec = len(tp) / len(got) if got else 0
    rec = len(tp) / len(GOLD)
    print(f"\n근거 검사 뒤: 정밀도 {prec:.3f}  재현율 {rec:.3f}")
    print("(앞 예제: 정밀도 0.636  재현율 0.778)")

    print(
        "\n세 검사가 각각 다른 걸 잡는다.\n"
        "  근거 문장 존재    — 모델이 문장 자체를 지어낸 경우\n"
        "  목적어 포함       — 문장은 진짜인데 값을 지어낸 경우 («업종: IT 서비스»)\n"
        "  단정 여부         — 원문이 «알려져 있으나»라고 했는데 사실로 바꾼 경우\n"
        "\n두 번째가 제일 많이 잡는다. 그리고 제일 늦게 깨달았다.\n"
        "처음엔 «근거 문장이 원문에 있는가»만 봤는데, «업종»이 통과했다.\n"
        "근거로 낸 문장 «가온테크는»이 원문에 진짜 있었기 때문이다.\n"
        "문장은 진짜고 값만 지어낸 것이다. 이게 제일 흔한 실패다.\n"
        "\n이 검사가 싼 이유는 모델을 한 번 더 부르지 않아서다.\n"
        "문자열 대조라 밀리초 단위고, 결과가 매번 같다.\n"
        "\n대가도 봐야 한다. 재현율이 0.778 에서 0.667 로 떨어졌다.\n"
        "«가온테크 체결 C-2025-118» 이 억울하게 걸렸다. 근거 문장이\n"
        "«가온테크는 2025년 6월 2일» 이라 계약번호가 그 안에 없었기 때문이다.\n"
        "맞는 트리플인데 근거 문장을 짧게 잘라 낸 게 문제였다.\n"
        "\n고치는 방법은 «근거 문장을 문장 단위로 내놓게» 하는 것이다.\n"
        "조각 단위로 자르지 말고 마침표까지. 프롬프트 한 줄이면 되고,\n"
        "이 한 줄이 재현율을 되돌려 놓는다.\n"
        "\n한계도 분명하다. 목적어가 문장에 «있기만» 하면 통과한다.\n"
        "관계가 뒤집힌 경우(«A가 B를 인수»를 «B가 A를 인수»로)는 못 잡는다.\n"
        "그건 모델을 한 번 더 불러야 한다. 값이 두 배가 되고 정밀도가 오른다."
    )


if __name__ == "__main__":
    main()
```

### `code/ex3_normalize.py`

```python
"""
예제 3 — 어휘 정규화. «뜻은 맞는데 이름이 다른» 트리플을 살려 낸다.

    python3 ex3_normalize.py

의존성 없음.
"""

from docs import GOLD, VOCAB_PREDICATES

# 모델이 실제로 내놓은 술어들 (여러 번 돌리면 이보다 훨씬 다양하다)
OBSERVED = ["체결", "계약체결일자", "체결일자", "계약일", "담당", "담당자",
            "책임자", "해지", "계약해지", "해지사유", "사유", "협업", "업종"]

# 사람이 만든 매핑 표. 이게 자산이다.
SYNONYM = {
    "계약체결일자": "체결일", "체결일자": "체결일", "계약일": "체결일",
    "담당자": "담당", "책임자": "담당",
    "계약해지": "해지", "사유": "해지사유",
}


def normalize(pred):
    p = SYNONYM.get(pred, pred)
    return p if p in VOCAB_PREDICATES else None


def main():
    print(f"{'모델이 낸 술어':<14} {'매핑 결과':<10} 판정")
    print("-" * 46)
    ok = unmapped = 0
    for p in OBSERVED:
        n = normalize(p)
        if n:
            ok += 1
            mark = "그대로" if n == p else f"→ {n}"
            print(f"{p:<14} {n:<10} {mark}")
        else:
            unmapped += 1
            print(f"{p:<14} {'—':<10} 어휘 밖 — 보류함에")

    print(f"\n매핑됨 {ok}개, 보류 {unmapped}개")

    print(
        "\n보류함이 이 파이프라인의 심장이다.\n"
        "«협업»과 «업종»이 여기 쌓인다. 그리고 주에 한 번 사람이 본다.\n"
        "\n볼 때 셋 중 하나를 정한다.\n"
        "  1. 어휘에 추가한다 — 실제로 필요한 관계였다\n"
        "  2. 동의어로 매핑한다 — 있는 술어와 같은 뜻이었다\n"
        "  3. 버린다 — 우리가 안 다루는 것이다\n"
        "\n이 결정을 SYNONYM 표에 쌓으면 다음부터 자동으로 처리된다.\n"
        "제 경우 3개월쯤 쌓으니 보류율이 18%에서 3%로 떨어졌다.\n"
        "\n중요한 건 «보류»가 «버림»과 다르다는 것이다.\n"
        "버리면 그 정보가 사라지고, 보류하면 나중에 살릴 수 있다.\n"
        "그리고 보류함 크기가 곧 «우리 어휘가 현실과 얼마나 어긋나는가»의 지표다."
    )


if __name__ == "__main__":
    main()
```

### `code/ex4_confidence.py`

```python
"""
예제 4 — 신뢰도를 어디서 얻을까. 모델이 말하는 확신은 믿을 게 못 된다.

    python3 ex4_confidence.py

의존성 없음.
"""

import random
from collections import defaultdict

random.seed(20260801)

# 같은 문서를 세 번 돌린 결과. 모델은 매번 조금씩 다르게 답한다.
RUNS = [
    {("가온테크", "체결", "C-2025-118"), ("C-2025-118", "담당", "김하늘"),
     ("가온테크", "업종", "IT 서비스"), ("C-2025-118", "상대", "나루소프트")},
    {("가온테크", "체결", "C-2025-118"), ("C-2025-118", "담당", "김하늘"),
     ("C-2025-118", "상대", "나루소프트"), ("C-2025-118", "체결일", "2025-06-02")},
    {("가온테크", "체결", "C-2025-118"), ("C-2025-118", "담당", "김하늘 부장"),
     ("C-2025-118", "상대", "나루소프트"), ("가온테크", "규모", "중견기업")},
]

# 모델이 스스로 매긴 확신도 (자기 보고)
SELF_REPORTED = {
    ("가온테크", "체결", "C-2025-118"): 0.95,
    ("C-2025-118", "담당", "김하늘"): 0.92,
    ("C-2025-118", "상대", "나루소프트"): 0.90,
    ("C-2025-118", "체결일", "2025-06-02"): 0.88,
    ("가온테크", "업종", "IT 서비스"): 0.91,     # 지어낸 건데 확신이 높다
    ("가온테크", "규모", "중견기업"): 0.87,       # 이것도
    ("C-2025-118", "담당", "김하늘 부장"): 0.85,
}

TRUE = {("가온테크", "체결", "C-2025-118"), ("C-2025-118", "담당", "김하늘"),
        ("C-2025-118", "상대", "나루소프트"), ("C-2025-118", "체결일", "2025-06-02")}


def main():
    votes = defaultdict(int)
    for run in RUNS:
        for t in run:
            votes[t] += 1

    print(f"{'트리플':<44} {'자기보고':>8} {'표 수':>6} {'실제'}")
    print("-" * 74)
    for t in sorted(votes, key=lambda x: (-votes[x], -SELF_REPORTED.get(x, 0))):
        label = " 참" if t in TRUE else " 거짓"
        print(f"{str(t):<44} {SELF_REPORTED.get(t, 0):>8.2f} "
              f"{votes[t]:>4}/3 {label}")

    print("\n두 신호를 임계로 갈라 보면")
    for name, key, th in (("자기 보고 0.90 이상", lambda t: SELF_REPORTED.get(t, 0), 0.90),
                          ("3번 중 3번 나옴", lambda t: votes[t] / 3, 0.99)):
        picked = {t for t in votes if key(t) >= th}
        tp = len(picked & TRUE)
        prec = tp / len(picked) if picked else 0
        rec = tp / len(TRUE)
        print(f"  {name:<20} 정밀도 {prec:.3f}  재현율 {rec:.3f}  "
              f"(뽑은 것 {len(picked)}개)")

    print(
        "\n«3번 중 3번»은 정밀도 1.000 인데 재현율이 0.500 이다.\n"
        "맞는 트리플 둘(체결일, 그리고 표기가 갈린 담당)을 놓쳤다.\n"
        "즉 표 세기는 «확실한 것만 고르는» 쪽으로 치우친다.\n"
        "\n자기 보고는 거짓에도 0.91, 0.87 을 준다. 참과 구분이 안 된다.\n"
        "여러 번 돌려 «몇 번 나왔나»를 세는 쪽이 훨씬 잘 갈라낸다.\n"
        "지어낸 것은 매번 다르게 지어내기 때문이다.\n"
        "\n대가는 값이다. 세 번 돌리면 값도 세 배다.\n"
        "\n제 절충안은 이렇다.\n"
        "  1차 — 한 번만 돌리고 근거 검사로 거른다 (싸다)\n"
        "  2차 — 근거 검사를 통과했지만 어휘 밖이거나 새 노드를 만드는 것만\n"
        "        두 번 더 돌려 표를 센다 (비싼 검사를 소수에만)\n"
        "\n«김하늘»과 «김하늘 부장»이 갈린 것도 눈에 띈다.\n"
        "표 수로는 2:1 인데, 이건 14장의 엔티티 해상도로 넘어갈 문제다.\n"
        "추출 단계에서 풀려고 하면 안 된다. 단계마다 할 일이 있다."
    )


if __name__ == "__main__":
    main()
```

### `code/ex5_incremental.py`

```python
"""
예제 5 — 문서가 바뀌면 트리플도 바뀐다. 무엇을 지우고 무엇을 남길까.

    python3 ex5_incremental.py

의존성 없음.
"""

V1 = {
    ("d01", "가온테크", "체결", "C-2025-118"),
    ("d01", "C-2025-118", "담당", "김하늘"),
    ("d01", "C-2025-118", "상대", "나루소프트"),
}
# 문서가 수정됐다. 담당자가 바뀌었고, 금액이 추가됐다.
V2 = {
    ("d01", "가온테크", "체결", "C-2025-118"),
    ("d01", "C-2025-118", "담당", "박서준"),
    ("d01", "C-2025-118", "상대", "나루소프트"),
    ("d01", "C-2025-118", "금액", "5000000"),
}
# 사람이 손으로 추가한 것 — 문서에는 없다
MANUAL = {
    ("manual", "C-2025-118", "비고", "갱신 협의 중"),
    ("manual", "C-2025-118", "담당", "김하늘"),   # 사람이 «김하늘이 맞다»고 정정
}


def naive_replace(old, new, doc):
    """흔한 방법: 그 문서에서 나온 것 전부 지우고 다시 넣는다."""
    kept = {t for t in old if t[0] != doc}
    return kept | new


def careful_merge(old, new, manual, doc):
    """사람이 손댄 것은 지키고, 나머지만 교체한다."""
    manual_keys = {(t[1], t[2]) for t in manual}
    kept = {t for t in old if t[0] != doc}
    incoming = {t for t in new if (t[1], t[2]) not in manual_keys}
    conflicts = {t for t in new if (t[1], t[2]) in manual_keys}
    return kept | incoming, conflicts


def show(title, s):
    print(f"\n{title}")
    for t in sorted(s):
        print(f"    [{t[0]:<7}] {t[1]:<12} {t[2]:<6} {t[3]}")


def main():
    old = V1 | MANUAL
    show("현재 상태 (문서 추출 + 사람 입력)", old)
    show("문서가 수정되어 다시 추출한 결과", V2)

    naive = naive_replace(old, V2, "d01")
    show("방법 A — 그 문서 것 전부 지우고 교체", naive)
    print("    → 사람이 정정한 «담당: 김하늘»이 살아 있다. 그런데...")
    dup = [t for t in naive if t[2] == "담당"]
    print(f"    → 담당이 {len(dup)}개가 됐다: "
          f"{[t[3] for t in dup]}  ← 충돌이 조용히 남는다")

    careful, conflicts = careful_merge(old, V2, MANUAL, "d01")
    show("방법 B — 사람이 손댄 항목은 지키고 충돌만 표시", careful)
    print(f"    → 충돌 {len(conflicts)}건을 사람에게 보낸다: "
          f"{[(t[2], t[3]) for t in conflicts]}")

    print(
        "\n방법 A 는 «담당»이 둘이 되고 아무도 모른다.\n"
        "질의가 둘 다 돌려주거나, 하나만 골라 주는데 어느 쪽인지 예측이 안 된다.\n"
        "\n방법 B 는 충돌을 드러낸다. 그리고 사람 입력을 함부로 덮지 않는다.\n"
        "\n핵심은 «출처를 트리플마다 저장해 두는 것»이다.\n"
        "출처가 없으면 무엇이 자동이고 무엇이 사람 것인지 구분할 수 없고,\n"
        "구분할 수 없으면 재추출할 때마다 사람 손댄 게 날아간다.\n"
        "\n제가 이걸 세 번 겪었다. 세 번째에야 출처를 저장하기 시작했다."
    )


if __name__ == "__main__":
    main()
```

### `code/extractor.py`

```python
"""
가짜 추출기. API 키 없이 돌리려고 만든 것이다.
진짜 모델이 흔히 보이는 실패 네 가지를 그대로 흉내 낸다.

  1) 지어내기      — 원문에 없는 사실을 만든다
  2) 어휘 벗어나기 — 정한 술어 대신 비슷한 말을 쓴다
  3) 추측을 사실로 — «알려져 있으나» 를 확정 문장으로 바꾼다
  4) 누락          — 문장 뒤쪽 사실을 빠뜨린다
"""

RAW_OUTPUT = {
    "d01": [
        ("가온테크", "체결", "C-2025-118", "가온테크는 2025년 6월 2일"),
        ("C-2025-118", "상대", "나루소프트", "나루소프트와 유지보수 계약을"),
        ("C-2025-118", "계약체결일자", "2025-06-02", "2025년 6월 2일"),   # 어휘 밖
        ("C-2025-118", "담당", "김하늘", "담당자는 김하늘 부장이다"),
        ("가온테크", "업종", "IT 서비스", "가온테크는"),                    # 지어냄
    ],
    "d02": [
        ("라온에너지", "해지", "2024-08", "2024년 8월 계약을 해지했다"),
        ("라온에너지", "해지사유", "예산 삭감", "예산 삭감을 이유로"),
    ],
    "d03": [
        ("다올물산", "대표", "최민준", "다올물산의 대표는 최민준이다"),
        # 소재지 누락 — 문장 뒤쪽을 빠뜨렸다
    ],
    "d04": [
        ("김하늘", "소속", "가온테크", "가온테크 김하늘 부장이"),
        ("가온테크", "협업", "나루소프트", "나루소프트와의 협업이 언급되었다"),  # 어휘 밖 + 근거 약함
    ],
    "d05": [
        ("마루상사", "모회사", "다올물산", "마루상사는 다올물산의 자회사로"),   # 추측을 사실로
    ],
}


def extract(doc_id):
    return RAW_OUTPUT.get(doc_id, [])
```

